import { styled } from "~stitches";

const Button = styled("button", {
  background: "none",
  border: "1px solid #E964FF",
  borderRadius: "10px",
  color: "#E964FF",
  padding: "1rem 3rem",
});

export { Button };
