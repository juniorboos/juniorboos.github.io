import { styled } from "~stitches";

const AppWrapper = styled("div", {
  backgroundColor: "#020C1B",
  padding: "0 24rem",

  display: "flex",
  flexDirection: "column",
  gap: "4rem",
});

export { AppWrapper };
